DB_USER=priya             # PostgreSQL username (create this role in PostgreSQL)
DB_HOST=localhost         # Database host (localhost)
DB_NAME=priya          # Database name
DB_PASSWORD=password # Password for the 'priya' role
DB_PORT=5432              # Default PostgreSQL port (optional)
PORT=5000                 # Server port (optional)
